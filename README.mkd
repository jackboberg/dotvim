Install:

    git clone git://github.com/jackboberg/dotvim.git ~/.vim

Create symlink:

    ln -s ~/.vim/vimrc ~/.vimrc

Fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
